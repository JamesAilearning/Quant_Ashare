# Walk-forward N=3 cross-fold ensemble experiment.
#
# Inherits everything from config_walk.yaml; only the two fields below
# differ from the baseline.  See config_walk.yaml for full documentation
# of every parameter.

extends: config_walk.yaml

ensemble_window: 3
output_dir: "output/walk_forward_industry_n3"
