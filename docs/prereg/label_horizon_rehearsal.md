<!-- Gate-rehearsal EVIDENCE for the 阶段6 label-horizon campaign (step 3.5).
     Generated by: python scripts/rehearse_label_horizon_gate.py
     Machine: operator box, 2026-07-03. Plan file committed at this branch's
     first campaign-prep commit; synthetic runs stamped with HEAD, clean. -->

# 阶段6 gate rehearsal — plan→run→compare full chain (CPU, synthetic runs)

Real `scripts/compare_walk_forward_runs.py` subprocess against synthetic
1-fold run dirs carrying the `daily_series` substrate. Three scenarios:

```
scenario 1 ACCEPT ok — ancestry verified, verdict printed.
scenario 2 FLAG ok — 10d flagged as unregistered.
scenario 3 REFUSE ok — dirty worktree refused, no verdict.

REHEARSAL PASS: plan->gate->verdict chain fully exercised (accept / flag / refuse). Ready for the 1-fold smoke, then the real runs.
```

Key gate lines observed verbatim (from the accept / flag / refuse runs):

```
  pre-registration GATE: PASSED (plan commit is an ancestor of both runs)
    expected_direction: treatment_better   registered variants: ['5d']
    ** FLAG:
       UNREGISTERED MULTIPLE COMPARISON: variant '10d' is not in the pre-registered set ['5d'] ...
  NO VERDICT (pre-registration gate failed):
    treatment run: the run was produced from a DIRTY worktree (git_dirty=True) ...
```

En-route finding worth keeping: a CONSTANT synthetic treatment edge is
refused by the ruler ("Degenerate CI ... zero sampling uncertainty") — the
fail-loud path works; the rehearsal series therefore carries a varying edge.
