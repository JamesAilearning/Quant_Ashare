<!-- Gate-rehearsal EVIDENCE for the 阶段6 label-horizon campaign (step 3.5).
     Generated by: python scripts/rehearse_label_horizon_gate.py
     Machine: operator box, 2026-07-03. Plan file committed on this branch;
     synthetic runs stamped with HEAD, clean. -->

# 阶段6 gate rehearsal — plan→run→compare full chain (CPU, synthetic runs)

Real `scripts/compare_walk_forward_runs.py` subprocess against synthetic
1-fold run dirs carrying the `daily_series` substrate and an embedded config
block (the gate derives ST-handling parity from it). Four scenarios:

```
scenario 1 ACCEPT ok — ancestry verified, verdict printed.
scenario 2 FLAG ok — 10d flagged as unregistered.
scenario 3 REFUSE ok — dirty worktree refused, no verdict.
scenario 4 REFUSE ok — ST-handling mismatch refused, no verdict.

REHEARSAL PASS: plan->gate->verdict chain fully exercised (accept / flag / refuse-dirty / refuse-st-mismatch). Ready for the 1-fold smoke, then the real runs.
```

Key gate lines observed verbatim (accept / flag / refuse-dirty / refuse-st):

```
  pre-registration GATE: PASSED (plan commit is an ancestor of both runs)
    expected_direction: treatment_better   registered variants: ['5d']
    ** FLAG:
       UNREGISTERED MULTIPLE COMPARISON: variant '10d' is not in the pre-registered set ['5d'] ...
  NO VERDICT (pre-registration gate failed):
    treatment run: the run was produced from a DIRTY worktree (git_dirty=True) ...
  NO VERDICT (pre-registration gate failed):
    ST-handling MISMATCH between the compared runs: baseline has st_mask_mode='off_experiment' ... vs treatment st_mask_mode='required' ...
```

En-route findings worth keeping: (1) a CONSTANT synthetic treatment edge is
refused by the ruler ("Degenerate CI ... zero sampling uncertainty") — the
fail-loud path works, so the rehearsal series carries a varying edge; (2) the
ST-parity refusal (codex P1 #323) is machine-enforced in the prereg gate — an
accidentally ST-on side can never receive a decision-grade verdict.
