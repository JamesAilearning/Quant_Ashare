## 1. Reproduce

- [x] 1.1 Add genuine scratch-Git source identities to rotation fixtures and reproduce dirty/unknown/unmerged-source installation before the fix.

## 2. Enforce source eligibility

- [x] 2.1 Reuse the pure source-field policy and require ancestry against the pinned revision on the existing bound sidecar, with classified bounded Git failures.
- [x] 2.2 Cover malformed source fields, ancestor/equality, branch movement, Git failures and zero-install refusal; document source-only scope.

## 3. Verify and publish

- [x] 3.1 Run targeted/full logic-governance, touched-module imports, pinned lint/type and strict OpenSpec validation serially.
- [x] 3.2 Complete independent local reviews to convergence and prepare a verified PR handoff requiring current-head Codex review and all CI before merge.
