# Tasks

## 1. 纯函数（`_daily_decision_helpers.py`）

- [x] 1.1 `find_nominal_baseline(artifacts, *, read_payload, as_of, limit)`：
      从 `as_of` 起向后找第一份可信的再平衡日工件
- [x] 1.2 六个分因常量（HOLD / 无节奏字段 / 节奏字段形状违约 / schema 不受支持 /
      建仓时点不合法 / 读不出来），逐条记账不静默跳过
- [x] 1.3 **不推断缺失**：没有 `rebalance_day` 键的老工件不当作再平衡日
      （`hold_state` 对它返回 `is_hold=False`，只看 `is_hold` 会误判）
- [x] 1.4 扫描上界 + `limit_reached` / `exhausted` 两态分开（「翻完了都没有」
      与「翻到上限就停了」对操作人的下一步不同）
- [x] 1.5 `baseline_roster`：只给代码集合。工件里没有权重/股数/金额，等权假设
      写进来就是凭空造仓位
- [x] 1.6 读盘由调用方**注入**：保持本模块零 I/O，读边界仍由页面那侧执法

## 2. 页面渲染

- [x] 2.1 位置在候选表**之前**——它框定下面所有内容的语境
- [x] 2.2 找到 → 日期 / 只数 / topk / universe / 代码表
- [x] 2.3 没找到 → 说清已回溯几份 + 明说「不等于没有持仓」
- [x] 2.4 跳过清单折叠展开，逐条给日期与原因
- [x] 2.5 撞上界时单独说明
- [x] 2.6 读盘走本页既有的 `read_json_artifact`（因此仍受 `guard_output_path`
      约束），不另开读路径

## 3. 红线守卫（机器执法）

- [x] 3.1 affordance 禁令：`st.text_area` / `st.number_input` /
      `st.data_editor` / `st.file_uploader` / `st.download_button` /
      `execCommand` / `navigator.clipboard`
- [x] 3.2 执行口径词只在**渲染字符串**里查——用 AST 取真正传给 `st.*` 的字面量
      （含 f-string 的字面段）。**首版写成全文串禁，当场被自己的免责声明judged
      红**（这一页本来就大量否定「买入 / 卖出 / 下单」），说明全文禁词是错的
      工具：它会逼出更弱的免责声明
- [x] 3.3 词表刻意**窄**：只收「除非真把功能做出来、否则没理由出现」的词
      （差分单 / 缓冲带 / 目标仓位 / 股数 / 手数）
- [x] 3.4 接线钉钉**条件整行**（`if _baseline.found:` 等）

## 4. 测试

- [x] 4.1 纯函数 15 条：六种分因各一 + 上界 + 翻完 + 从选中日起算 + 选中日自己
      可以是基准 + 名单只有代码 + 损坏 payload **抛**而不是给空名单
- [x] 4.2 拿**本机真实工件**跑一遍（worktree 无 `output/` 时 skip，不伪造通过）
- [x] 4.3 实测结论：本机 `2026-07-31` 是 HOLD、`2026-06-16` schema 不受支持 ⇒
      **当前没有可信基准**。这正是这一页今天回答不了、而操作人需要知道的事
- [x] 4.4 变异复验 **17 条全咬住**（含「老工件被当成再平衡日」「HOLD 日也当基准」
      「扫描无上界」「撞上界谎报成翻完了」「忽略 as_of」「缺代码的候选被静默丢弃」
      「给名单加导出按钮」「渲染出执行口径词」）
- [x] 4.5 `tests/logic` + `tests/governance` **5064 passed**；ruff /
      mypy --strict / openspec validate --strict 全绿

## 4b. codex #475 第二轮：四条 P1/P2

首版的回溯是同一批工件上的**第二条、更弱**的校验路径——页面对**选中**的那份
工件会 `st.stop()` 的形状，回溯却放行并当成可信基准。四条都指向这一点：

- [x] 4b.1 **P1 读不出来的较新工件不能翻过去。** 那一份**本身可能就是一次更近
      的再平衡**；翻过它把更早那张单报成当前基准，就是拿一张**可能已被取代**
      的清单当此刻该持有的。→ 回溯**就地停下**，判为「不可知」
- [x] 4b.2 **这条推广成了整段语义的总闸：只有经过校验的 HOLD 才许可继续往回
      翻。** 它是唯一能证明「那天没换手、所以更早那张单仍然有效」的证据。
      读不出来 / schema 不认 / 日期对不上 / meta 损坏 / 没有节奏字段 / 节奏
      字段形状违约——每一种都让证据链断在这里
- [x] 4b.3 **P1 文件名日期 ↔ payload as_of_date 必须一致。** 改名/拷贝过的
      工件会让「八月三日那一份」其实装着八月十日的截面——拿它当基准就是把
      **未来数据**当成当日应持有。选中工件流早有这道闸（`daily_decision.py`
      的日期一致性闸），回溯此前没有
- [x] 4b.4 **P1 带 v2 标记却没有 dict meta 的工件必须拒绝。** 产出器对 v2 恒
      写 dict meta，选中工件流对同一形状 `st.stop()`，回溯此前放行
- [x] 4b.5 **P2 损坏的名单不是空名单。** 首版 `except ValueError: roster = ()`
      之后页面接着说「名义上跟的是 X 那次的清单（共 **0** 只）」——把一份损坏
      工件渲染成一个**合法的空仓位**，而这一页别处对 picks 形状违约的处置正是
      「要被看见，不要被渲染成良性空缺」。→ 整段改判不可用
- [x] 4b.6 页面从两态改成**三态互斥**：找到 / **不可知**（回溯停在哪一份、
      因为什么）/ 翻完了都没有。「不知道」与「确实没有」对操作人的下一步完全
      不同
- [x] 4b.7 本机真实工件复跑：结论从「翻完了、没找到」变成
      **「不可知：回溯在 2026-06-16 停下——schema 不受支持」**。严格更诚实——
      我们并**没有**翻完，是证据链断了
- [x] 4b.8 变异复验 **21 条全咬住**（新增：阻断条件熄火 / 读不出来被翻过去 /
      四道新闸各熄火 / 损坏名单退成空名单 / 不可知与翻完了混成一句）

## 4c. codex #475 第三轮：两条 → 整类下沉

codex 点了两条：节奏双字段只带其一必须**阻断**回溯；重复 `stock_code` 不能
当成「2 只候选」的基数。两条都是同一形状——回溯**仍然**是同一批工件上的第二条
更弱路径，只是这次差的不是四道闸而是八道。照着补那两条，剩下六道就是下一轮的
同类意见。

- [x] 4c.1 把整类**下沉**成共用函数 `producer_shape_violation(payload, *,
      as_of_date, entry_date)`，放在 `_daily_decision_helpers.py`（低层）。
      `_today_workbench_helpers.py` 的同名闸**删掉**改为调用它——两处不再各
      写一份。工作台既有用例逐字保留消息文本，全绿即证明行为未变
- [x] 4c.2 覆盖的八道：行级六键六型 / 重复代码 / rank 连续 1..N / 分降序 /
      `meta.topk` 界 / 节奏双字段成对 / 再平衡日 `next == as_of` /
      HOLD 的 `next` 是不早于 entry 的严格 ISO 工作日
- [x] 4c.3 **刻意不含来源判据**（`provenance_verdict`）。那道闸问的是「这份
      工件是不是**现任**模型出的」——历史基准出自上一代模型是合法的，拿现任
      身份拒它是错的。形状合约与身份归属是两个问题
- [x] 4c.4 `baseline_roster` 自己也拒重复代码：那个元组的长度就是页面上的
      「共 N 只」。报数的那一处不能指望调用链上游都验过
- [x] 4c.5 **两条防漂移钉**：分别替换共用闸的实现，回溯与工作台**各自**必须
      跟着变。否则「两处同一道闸」只是注释里的说法
- [x] 4c.6 测试 fixture 补成产出器真产得出的形态（六键齐全、节奏双字段同写）。
      **首版 fixture 本身就是产出器产不出的**——一份产不出的 fixture 证明不了
      任何读侧行为，这正是这道闸此前看不见的原因
- [x] 4c.7 `BASELINE_BLOCK_NO_ENTRY_DATE` 加完发现**不可达**（上游
      `artifact_entry_timing_is_valid` 已证 entry 是严格 ISO）→ **删掉**，
      改成一条钉那个前提的用例（闸序被调换时它会红）
- [x] 4c.8 变异复验 **31 条全咬住**。首轮逃掉 1 条：`cadence.malformed`
      在工作台侧**从来没有用例**（本 change 之前就没有，下沉把它暴露出来）
      → 补上工作台的非布尔 `rebalance_day` 用例
- [x] 4c.9 真实工件复跑：结论不变（`2026-07-31` HOLD → 停在 `2026-06-16`
      schema 不受支持 ⇒ 不可知），但这次那份 HOLD 是过了**完整**产出器形状
      合约的，不再是过了一个子集

## 5. 划界（本 change 不做）

- 不做基准名单 × 当日 top-K 的成员对照（下一个 PR，依赖本 PR 的基准定位）。
- 不做跨日决策履历浏览（再下一个；它要能在工件缺失时照常工作，会打破「页面由
  工件驱动」这个现有前提）。
- 不碰决策日志的 schema 或写入路径。
- 不引入 `src.inference.rebalance_schedule`：节奏只读产出器写下的字段。
