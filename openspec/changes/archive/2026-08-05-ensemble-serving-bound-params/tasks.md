# Tasks: 2026-08-05-ensemble-serving-bound-params

## 1. 实现（单 PR）
- [x] CLI 三参数 None 哨兵化 + ensemble 模式绑定解析（等值放行/
      不等拒/绑定源缺失拒）+ legacy 缺省逐字保留
- [x] runbook 晨跑命令改写（周节奏卡 + 首次上线卡第 6 步）
- [x] 测试：绑定/等值/不等拒/缺绑定源拒/legacy 不变
- [x] openspec validate --strict + ruff/mypy/目标测试 + 全量快套
- [x] codex review 循环 + CI 绿 → STOP 等 merge
