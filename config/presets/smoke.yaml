# Smoke preset — quick sanity check (5-10 min)
mode: pipeline
instruments: csi300
feature_handler: Alpha158
model_type: LGBModel
compute_device: cpu
num_boost_round: 200
early_stopping_rounds: 30
learning_rate: 0.05
# Canonical total-return benchmark SH000300TR (applied at REGEN-2). SH000300 = REGEN-A price control.
benchmark_code: SH000300TR
topk: 30
n_drop: 3
signal_to_execution_lag: 1
