# 阶段8 Gate-4 FINAL-ADJUDICATION run config for candidate C1_GPA — FROZEN (#352).
# codex r11 P1: the gate binds --candidate to the config chain's
# gate3_candidate (child-first), so an unbound CLI claim can no longer
# gate one candidate and run another. codex r12: until the Gate-4B
# augmentation wiring CONSUMES this key, run_walk_forward.py REFUSES to
# execute any gate3-bound config (fail-loud guard, pinned by
# tests/logic/test_run_walk_forward_mined.py) -- no plain-Alpha158 run
# can masquerade as a candidate run. Parent = frozen snapshot.
extends: quality_gate3_final_adjudication.yaml
gate3_candidate: C1_GPA
